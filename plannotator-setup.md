# Plannotator Setup

To install Plannotator, run the following command:

```bash
curl -fsSL https://plannotator.ai/install.sh | bash
```

This will download and execute the Plannotator installation script.