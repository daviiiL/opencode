# opencode config

## prereqs

- Node.js

## do before

```bash
git submodule init
git submodule update
```

This will sync the `plugin/shell-strategy` submodule from `https://github.com/JRedeker/opencode-shell-strategy.git`.
